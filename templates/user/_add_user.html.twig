
{{ form_start(add_form, {
    'enctype': 'multipart/form-data',
    'attr': {'id': 'add-form'}
}) }}
<div class="row align-items-center">
    <div class="col-md-12">
        <div class="row mb-3">
            <div class="col-md-6 ">
                <p>email :</p>
                {{ form_label(add_form.email, null, { label: false} ) }}
                {{ form_widget(add_form.email, { 'attr': {  style:"width: 35vw;"}}) }}
                {{ form_errors(add_form.email) }}
            </div>
            <div class="col-md-6">
                <p>mot de passe :</p>
                {{ form_label(add_form.password, null, { label: false} ) }}
                {{ form_widget(add_form.password, { 'attr': {  style:"width: 35vw;"} }) }}
                {{ form_errors(add_form.password) }}
            </div>
        </div>
        <div class="d-flex justify-content-end gap-2">
            <p>email vérifié :</p>
            {{ form_row(add_form.isVerified, 
                 { label: false, attr: { disabled: 'disabled' }} ) }}
            {{ form_row(add_form._token) }}
            <input type="hidden" name="add_mode" value="1">
            <input type="hidden" name="storage" value="orm">
            <button type="submit" class="btn btn-outline-dark d-flex align-items-center justify-content-center" id="button" style="width:15vw; height:5vh">ajouter</button>
        </div>
    </div>
</div>
{{ form_end(add_form, { 'render_rest': false }) }}