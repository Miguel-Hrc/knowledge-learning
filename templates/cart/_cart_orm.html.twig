    
    {% for course in courses %}
        <div id="container" class="row d-flex text-center justify-content-between" style="margin: 2vw; background-color: #FFFFFF; width: 75vw; height: 30vh">
            <div id="container" class="col-4  d-flex align-items-center justify-content-center" style=" background-color: #F1F8FC; width: 20vw; height: 25vh">
                <p>{{ course.theme.name }} </p>
            </div>
            <div id="container" class="col-4 d-flex align-items-center justify-content-center" style=" background-color: #F1F8FC; width: 20vw;height: 25vh">
                <p>{{ course.title }} </p>
            </div>
            <div id="container" class="col-4 d-flex align-items-center justify-content-center" style=" background-color: #F1F8FC; width: 20vw; height: 25vh">
                <p>{{ course.price }} €</p>
            </div>
        </div>
    {% else %}
        <div id="container" class="row d-flex text-center justify-content-between" style="margin: 2vw; background-color: #FFFFFF; width: 75vw; height: 30vh">
            <p>Aucun cursus</p>
        </div>
    {% endfor %}
    </div>
    {% for lesson in lessons %}
        <div id="container" class="row d-flex text-center justify-content-between" style="margin: 2vw; background-color: #FFFFFF; width: 75vw; height: 30vh">
            <div id="container" class="col-3  d-flex align-items-center justify-content-center" style=" background-color: #F1F8FC; width: 15vw; height: 25vh">
                <p>{{ lesson.course.theme.name }} </p>
            </div>
            <div id="container" class="col-3 d-flex align-items-center justify-content-center" style=" background-color: #F1F8FC; width: 15vw;height: 25vh">
                <p>{{ lesson.course.title }} </p>
            </div>
            <div id="container" class="col-3 d-flex align-items-center justify-content-center" style=" background-color: #F1F8FC; width: 15; height: 25vh">
                <p>{{ lesson.title }}</p>
            </div>
            <div id="container" class="col-3 d-flex align-items-center justify-content-center" style=" background-color: #F1F8FC; width: 15vw; height: 25vh">
                <p>{{ lesson.price }} €</p>
            </div>
        </div>
    {% else %}
        <div id="container" class="row d-flex text-center justify-content-between" style="margin: 2vw; background-color: #FFFFFF; width: 75vw; height: 30vh">
            <p>Aucun leçon</p>
        </div>
    {% endfor %}
    
